# GraphQL queries to fetch character details using character(id: ID!) field

# Query for character with id: 1
query GetCharacter1 {
  character(id: 1) {
    id
    name
    status
    species
    type
    gender
  }
}
